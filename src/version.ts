/**
 * Application version
 * 
 * This is the single source of truth for the displayed version in the app.
 * When updating, also update:
 * - package.json (version field)
 * - src-tauri/tauri.conf.json (version field)
 */
export const APP_VERSION = '2.3.2';
